# 謎解きサイトの作り方と注意点 ― 次に作るときの手引き

「応中の封印を解け！」（2026年8月・実施済み）で実際に踏んだ落とし穴と、
そこから分かったことをまとめたもの。次回、AIと一緒に読み返して使う前提で書いてある。

このファイルは**プロジェクト固有の説明書ではない**（それは `README.md`）。
ここに書くのは「次に別の謎解きを作るときにも効く話」だけ。

> **あわせて読む**：`作る前に持っておきたい視点.md`
> こちらは技術的な落とし穴をまとめたもの。
> 「どう見れば最初からいい感じに作れるか」（見せ方・順番・運用の考え方）は
> そちらにまとめてある。**着手前に読むならあちらが先**。

---

## 0. 最初に読む・3つの結論

この一件でいちばん高くついた失敗を3つに絞ると、こうなる。

1. **実機で確かめないまま5回直して、5回とも外した。**
   → 手元で再現できない環境の不具合は、憶測で直さず**測る道具を先に作る**。
2. **印刷物を「画面で見て」OKにしていた。**
   → 実際は8枚が16枚になり、QRは規格違反だった。**PDFに書き出して数える**。
3. **URLのパラメータを状態の正解として信用していた。**
   → ブラウザの「戻る」だけで復活し放題になっていた。**正解はlocalStorageに置く**。

---

## 1. 全体の流れ

```
企画 → 謎を決める → データを1ファイルに集約 → 画面を作る
     → 印刷物を作る → 実機テスト → 本番前チェック → 当日
```

### 1-1. 謎のデータは1ファイルに集約する

今回は `js/puzzles.js` に、謎文・選択肢・正解・ヒント・手紙の本文・
クリア後のメッセージまで全部入れた。これが効いた場面：

- 本番前日に扉の謎の答えが `135` → `216` に変わったとき、**1行直すだけ**で済んだ
- 一通目の手紙（`PROLOGUE`）は、参加者用ページ・スクリーン投影用ページ・
  紙に印刷する予備用PDF の**3か所で同じ変数を読んでいる**ので、
  文面を直せば3つとも自動で揃う

> **原則**：同じ文章・同じ数字が2か所に書かれていたら、それは将来ずれる。

### 1-2. 「本番用の完成品」と「作るための道具」を分ける

- `tools/qr-posters.html` … 開いたらそのまま印刷できる**完成品**
- `tools/qr.html` … 運営が中身を確認するための**道具**

最初これを1つにしようとして、「URL入力欄と生成ボタンのある汎用ツール」を
作りかけた。実際に必要だったのは「今日使う紙が出てくるページ」だけだった。

> **原則**：一度しか使わないなら、汎用化しない。

---

## 2. 実機でしか分からないことがある（最重要）

### 2-1. 何が起きたか

「画面を切り替えても、前の画面で拡大した倍率が残る」というiPhoneの不具合。
これを直すのに**5回失敗した**。毎回「直りました」と報告して、毎回直っていなかった。

原因は単純で、**検証環境でiOS Safariを再現できていなかった**こと。
ヘッドレスChromiumのUAをiPhoneに偽装しても、iOS Safariのズーム挙動は再現されない。
「Chromiumで直った」＝「iPhoneで直った」ではない。

### 2-2. 転機になったやり方

6回目に、直すのをやめて**測る道具を作った**。

- `window.visualViewport.scale`（今の拡大率）を画面に出すだけのテストページ
- 「A: 新しいURLへ遷移」「B: reload」「C: metaタグ差し替え」「D: viewport幅を変える」の4ボタン
- これを実機で押してもらい、**「Aだけ直る、他は変わらない」**という一次情報を得た

これで初めて正解にたどり着いた。

### 2-3. 分かった事実（iOSの仕様）

- iOS Safari 10以降は、アクセシビリティのため
  `user-scalable=no` / `maximum-scale` を**意図的に無視する**。
  metaタグを差し替える定番の小細工は**効かない**。
  （しかも拡大中に何度も差し替えると再レイアウトが重なって**固まる**）
- JavaScriptからピンチ拡大率を戻すAPIは**存在しない**。
- 等倍に戻る唯一の方法は「**新しいページを実際に読み込むこと**」。
- ただしiOSは**URLごとに拡大率を覚えている**（スクロール位置と同じ扱い）。
  `?stage=kozo` のような固定URLだと、2回目以降は前の倍率が復元される。
  → 毎回ちがうURLにするため、使い捨てのパラメータを足す。

```js
// js/zoom-fix.js
window.gotoFresh = function (url) {
  var sep = url.indexOf("?") === -1 ? "?" : "&";
  location.href = url + sep + "z=" + Date.now().toString(36);  // 毎回ちがうURL
};
```

これに伴い、**「アドレスは変えずに画面だけ差し替える」作りをやめた**。
`el.hidden = false` でカードを出し分けていた所を、全部ページ遷移に置き換えている。

### 2-4. 教訓

> **同じ症状の報告を2回受けたら、直すのをやめて測る道具を作る。**
> 3回目以降の「たぶんこれで直る」は、ほぼ当たらない。

> **「自分の環境で確認した」と「実機で直った」は別物。**
> 再現できない環境の話は、そう言ってから進める。

---

## 3. 印刷物の落とし穴

画面で見て綺麗でも、紙にすると壊れる。**必ずPDFに書き出して確認する**。

### 3-1. 8枚のつもりが16枚出る

`@page` を書かないと、ブラウザ既定の余白（約10mm）が入る。
210×297mmのデザインは版面に収まらず、**1枚が2ページに割れる**。

```css
@page { size: A4; margin: 0; }

@media print {
  .sheet:last-child { page-break-after: auto; }  /* 末尾の空白ページ対策 */
}
```

確認方法（これをやれば一発で分かる）：

```js
await page.pdf({ path: 'out.pdf', format: 'A4', printBackground: true, preferCSSPageSize: true });
// → ページ数を数える。8枚のつもりなら8ページのはず
```

### 3-2. QRコードの静穏領域（クワイエットゾーン）

QRの周囲には**4モジュール分の余白**が規格で必要。ライブラリの既定値を
`0` に潰してしまい、規格の6割しか確保できていなかった。

```js
qr.createSvgTag(6, 24);   // 第2引数 = 余白。cellSize 6 × 4モジュール = 24
// qr.createSvgTag(6, 0);  ← これがダメだった
```

体育館の斜光、蛍光灯の映り込み、古い端末——条件が悪いときに
読めない個体が出る。**ここはケチる場所ではない**。

### 3-3. 背景が白紙になる

濃い背景のデザインは、印刷設定の「背景のグラフィック」がOFFだと消える。
**注意書きで人間に頼らず、CSSで強制する**（8枚全滅する単一障害点なので）。

```css
.poster { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
```

### 3-4. 紙のフチは印刷できない

家庭用・学校のプリンタは紙端3〜5mmに印字できない。
紙いっぱいに枠線を引くと、**枠が欠ける／左右非対称になる**。
→ 外周に5mmの白フチを設けて、その内側にデザインを収める。

### 3-5. 乱数を使うと「同じ見た目」でなくなる

会場に貼る8枚は「どのQRがどの謎か分からない」ことが要件だった。
なのに星や文字の配置を `Math.random()` で散らしていたため、
**8枚とも違う絵**になっていた。模様の違いで見分けられてしまう。

→ 座標を固定値の配列にした。検証は「QR以外のマークアップが
バイト単位で一致するか」で行う：

```js
const c = poster.cloneNode(true);
c.querySelector('.poster-qr-frame').innerHTML = '';  // QRだけ除外
// → 8枚分の innerHTML が全部一致すればOK
```

> **原則**：「全部同じ」が要件なら、乱数は使わない。

### 3-6. 隠したい情報の扱い

掲示物に「中謎② 昇降口」と書いてしまうと参加者にバレる。
かといって何も書かないと、貼るスタッフが分からない。

→ **切り離せる「耳」**を紙の下端に付け、貼ったら切り取って捨てる運用にした。

このとき注意：耳をポスターの**内側**に重ねて置くと、ポスターの外枠・
四隅の飾りが耳の下に隠れて印刷されない。
紙（`.sheet`）の中に「ポスター」と「耳」を**上下に並べる**構造にすること。

---

## 4. 状態の持ち方（不正・事故を防ぐ）

### 4-0. 前提：進捗は「チーム」ではなく「端末」にひもづく

進捗は `localStorage` に持たせている。つまり実体は
**チームではなく、その端末（ブラウザ）1つ**に紐づいている。
今回は「1チーム1台」で運用しており実害はなかったが、次回
「同じチームの複数人がそれぞれ自分のスマホで読み進める」運用にするなら、
これは無視できない設計前提になる。

- 2台目の端末で最初にQRを読むと、同じチーム名を入力しても
  システム上は**別チームとして新規登録**される
- 2台の間で解いた進捗は同期しない（GASへは記録用に一方通行で送るだけ）
- 4-4の不具合（ルートA/B）も、実際には「2台目の端末が入口QRを
  経由せず設置QRを直接読んでしまった」ことが引き金だった可能性がある

> **原則**：「1チーム＝1台」を前提にするなら、それを**仕様として明言し、
> 運用（受付で1台に絞ってもらう）でも徹底する**。もし次回
> 複数端末を許容したいなら、進捗をチーム単位でサーバ（GAS）側に
> 持たせる設計に変える必要がある——localStorageのままでは対応できない。

### 4-1. URLパラメータを信用しない

「不正解のペナルティ（呪い）は、先生のQRを読まないと解除できない」
という仕様だったが、実装が

```js
if (params.get("revived") === "1") { state.cursed = false; }   // ← 危険
```

になっていた。一度復活すると `point.html?p=1&revived=1` が履歴に残り、
**再び呪われても「戻る」ボタンでそのURLに戻るだけで復活できた**。

→ 解除できるのは復活ページだけ（localStorageを書き換える側）にし、
表示側は**URLを一切見ず、保存済みの値だけで判断する**ようにした。

> **原則**：URLは「どこを表示するか」の指示。「何が起きたか」の証拠にはならない。

### 4-2. 再現テストの書き方

この手の不具合は、手順を実際になぞって確かめる。

```
誤答 → 呪い → 復活 → 誤答 → 呪い → 戻るボタン → 謎が解ける状態か？
```

修正後、同じ手順でブロックされることを確認した。

### 4-3. 別タブとの同期

OSのカメラアプリでQRを読むと、**新しいタブで開く**。これはブラウザ／OSの
仕様でサイト側から止められない。元のタブは呪われたまま取り残される。

→ `storage` イベントで、別タブのlocalStorage更新を検知して自動で追従させた。

```js
window.addEventListener("storage", function (e) {
  if (e.key !== Store.KEY) return;
  var latest = Store.load();
  if (latest && !latest.points[p].cursed) { /* 続きの画面へ */ }
});
```

**注意**：プライベートモードでは、ブラウザによって別タブ間でlocalStorageが
共有されない。参加者には「プライベートモードを解除」と案内すること
（今回は注意事項ページに明記した）。

### 4-4. 「入口を通った前提」の状態は、入口を通らなかった経路も想定する（本番で実際に発生）

ルートA・Bは、参加者を2グループに分けて時間差をつけるための仕組み。
`index.html?skip=1&route=A`（①始まり＝a→b→c→d→e）と
`?route=B`（③始まり＝c→d→e→a→b）の**2種類の入口QR**から始まる前提で、
`team.startRoute` にその起点を保存し、巡回順チェック（`point.html`）と
「次に向かう場所」表示（`index.html`）の両方がそれを読む——という設計自体は正しかった。

ただし**チームの初回登録が必ずこの入口QRを経由する保証がなかった**。
設置QR（`point.html?p=N`）を、登録前の端末（未登録のサブ端末や、
入口QRを踏まずに直接その地点に来てしまった場合など）でいきなり読むと：

```js
// point.html：team が無い（未登録）ときのフォールバック
location.replace("index.html?next=" + encodeURIComponent(p));   // ← route情報を持っていない
```

`next=3` だけを持って `index.html` に来る。この画面では

```js
let assignedRoute = (route && START_ROUTES[route]) ? START_ROUTES[route] : null;
// route（URLパラメータ）が無いので assignedRoute は null のまま
```

となり、登録時に

```js
if (assignedRoute) { team.startRoute = assignedRoute; Store.save(team); }
```

の `if` を素通りして**`team.startRoute` が一度も設定されない**。
未設定の `team.startRoute` は巡回順チェック側で
`team.startRoute || POINT_ORDER[0]`（＝①）にフォールバックするため、
③から始まったはずのチームが「①からスタートした扱い」になり、

1. ホーム画面に「次に向かう場所」が一度も出ない（`team.startRoute` が偽のため）
2. 最初に向かった地点（③）で謎を開こうとすると、巡回順チェックが
   「まだ①をやっていない」と判定し、`wrongOrderCard`（エラー画面）が出る
3. 結局①に行くと解けてしまう（フォールバック先と一致するため）

という現象になった。本番中に実際に報告された
（「Bルートを読んだのにAの謎しか受け付けなかった」）。

**このバグが見つけにくかった理由**：ルートA/B自体のコードパス
（入口QR→小謎→登録、を一直線にたどった場合）は何度読み返しても正しい。
穴は「入口QRを経由しない登録」という**設計上想定していなかった経路**にあった。
静的サイトを役割ごとに複数ページへ分けると、こういう「ある画面の前提が、
別の画面からの遷移では満たされない」抜け道ができやすい。

→ 修正は、登録時に `assignedRoute` が無ければ、これから向かう地点
（`next`）を起点として `team.startRoute` に採用するようにした
（＝どこから登録しても「その人が実際にいる場所」を起点として扱う）。

> **原則**：「この状態は必ずこの入口を通ってから来る」という前提は、
> ページ数が増えるほど崩れやすい（別ページからの直リンク、二台目の端末、
> ブックマーク、共有URLなど）。前提が崩れたときに**黙ってデフォルト値に
> フォールバックする**設計は危険——正常に見えて実は違う状態で進んでしまう。
> フォールバックさせるなら、そのときの文脈（今回なら「これから向かう地点」）
> から**その場で正しい値を作り直す**方を選ぶ。

---

## 5. 音

- 音声ファイルを持たず、**Web Audio APIで合成**した。
  静的サイトにアセットを置かずに済み、ライセンスも気にしなくてよい。
- **ブラウザの自動再生ポリシー**により、音はユーザー操作（クリック等）の
  ハンドラの中でしか鳴らない。ページを開いた瞬間に鳴らすことはできない。
  → 今回は「封蝋をクリックして開封」の中で鳴らした。

### 聴けない環境で音を検証する方法

AIは音を聴けない。それでも「ちゅんちゅん鳥みたい」という指摘には対処できた。

`OfflineAudioContext` で実際にレンダリングし、数値で比べる：

| | 高域の突発的な立ち上がり | 最大/平均比 |
|---|---|---|
| 変更前 | 6回 | 7.26 |
| 変更後 | 1回 | 3.56 |

「高い音が短く跳ねる回数」が鳥のさえずり感の正体だった
（1400〜3200Hzの単発音を、ピッチを上げながら鳴らしていた）。

> ただし**測定は聴感の代わりにはならない**。最後は必ず人が聴いて確認する。

---

## 6. レイアウト・CSSでよくやるミス

| 症状 | 原因 | 対処 |
|---|---|---|
| 大きさの違うボタンが上下にずれる | `inline-block` はベースラインで揃う | `display:flex; align-items:center` |
| スマホ横向きでボタンが画面外に出る | 高さが足りない（横向きは高さ約390px） | `@media (orientation:landscape) and (max-height:540px)` で縮める |
| 背景の飾りが前面に出る | 飾りに `z-index` を付けて浮かせていた | 親に `z-index` を付けず、DOM順で沈める |
| 同じ文言が2か所に出て混乱する | 「手順」と「見つけた謎」の両方に指示文を表示 | 見つけたら手順側から消す |

### 実機サイズの目安（テストに使う）

| 端末 | 縦 | 横 |
|---|---|---|
| iPhone SE | 375×667 | 667×375 |
| iPhone 14 | 390×844 | 844×390 |
| iPad | 768×1024 | 1024×768 |

**横向きは高さが400px程度しかない**。縦長前提で作ると必ず溢れる。

---

## 7. 参加者がつまずく所には必ず逃げ道を

今回用意したフォールバック：

| 本来の手段 | 使えないとき |
|---|---|
| QRを読む | 番号を手入力（中謎 1〜5／ジャンプ 101〜103） |
| サイト内カメラ | OSのカメラアプリ（別タブ対応済み） |
| スマホで手紙を表示して重ねる | 実物大に印刷した紙の手紙（`letter_print.pdf`） |
| 自力で解く | ヒントボタン → 近くの先生 |

**紙のフォールバックは実寸校正が命**。
手紙の幅84.93mm＝パンフレットの魔法陣中心間62.0mmで合わせてある。
印刷時に「用紙に合わせる」を選ぶとずれて謎が成立しないので、
プリント自体に「必ず100%で印刷」と刷り込んでおくこと。

### 参加者に出す文言には、答えのヒントを混ぜない

掲示物・注意事項・番号入力の案内は8枚とも共通にし、
個別の謎に関する情報は一切書かない。
（番号の範囲すら出さない方が安全だったので、最終的に
「QRが読み取れない時は近くの先生へ！」だけにした）

---

## 8. 本番前チェックリスト

```
□ デバッグ機能を切る（js/config.js の DEBUG_ENABLED を false）
□ 検証用に作った一時ファイルを消す
□ 印刷物をPDFに書き出し、ページ数・用紙サイズを数える
□ QRを全部デコードして、正しいURLに対応しているか確認
□ 実機（iPhone / Android 各1台）で最初から最後まで通す
□ 印刷済みの配布物と、サイト側の答え・文言が一致しているか
□ 派生物（PDF等）を作り直したか ※元のHTMLを直したら要再生成
□ 当日使う端末のプライベートモードを解除
□ スクリーン投影用ページの音量を実機で確認
□ リンク集を1枚にまとめ、1つずつ実際に開いて確かめる
```

### 「派生物の作り直し」に注意

今回、`pamphlet.html` の文言を「転校生→新入生」に直したが、
そこから書き出した `pamphlet.pdf` は古いままだった。
**HTMLを直したら、そこから作ったPDF・画像も作り直す**。

### リンク集は必ず1つずつ開いて確かめる

「みんなで見る用の手紙」として `letter.html?replay=1` を案内していたが、
正しくは `letter-opening.html`（音付き・スクリーン投影用）だった。
似た名前のページが複数あると、記憶で答えると間違える。

---

## 9. AIと一緒に作るときのコツ

### 効いたこと

- **役割を分けて意見を出させ、最後に批評役を1人立てる**。
  今回はポスターのデザインで、背景／格式／いたずら／文字組の4人に案を出させ、
  5人目に印刷物として成立するか批評させた。
  その批評が、上の「8枚が16枚になる」「QRが規格違反」を見つけた。
  **作った本人は自分の穴を見つけにくい。**
- **数値で検証させる**。「綺麗にできました」ではなく
  「ページ数8、QR8件デコード成功、マークアップ一致」まで出させる。

### 気をつけること

- AIは**実機の挙動を再現できない**（iOS Safari、実際の印刷、音）。
  「確認しました」の中身が何なのか（どの環境か）を必ず言わせる。
- 症状が繰り返すときは、**修正案ではなく検証方法を要求する**。
- 一括置換（`転校生→新入生` など）のあとは、**派生物と画像内の文字**を忘れやすい。

### 依頼の仕方

具体的な指示ほど良い結果になった。実例：

- ✅「切り取りの上までをポスターにして。今のままだと下部のデザインが切り取られる」
  → 症状と原因の推測がセットで、すぐ直せた
- ✅「ちゅんちゅん、鳥の鳴き声みたいなやつ消して。ふわーっと魔法にかけられる感じ」
  → 擬音でも、消すべき対象が特定できるので十分伝わる

---

## 10. この作品で使った構成（参考）

```
静的サイト（HTML/CSS/JS のみ）＋ GitHub Pages
  └ 進捗の保存 … localStorage（端末内）
  └ 運営の集計 … Google Apps Script + スプレッドシート
  └ QR生成 …… js/vendor/qrcode-generator.js（MIT・リポジトリ内に同梱）
  └ 音 ……… Web Audio APIで合成（音声ファイルなし）
```

**外部CDNに依存しない**方針にした（本番前夜に外部サイトの生死へ依存したくない）。
QRライブラリはCDNから読んでいたが、リポジトリ内に取り込んだ。

---

*作成：2026年8月・「応中の封印を解け！」実施後*
